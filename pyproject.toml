[project]
name = "smartdialer-prototype"
version = "0.1.0"
description = "Safety-first progressive and predictive smart dialer prototype"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
smartdialer = "smartdialer.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
